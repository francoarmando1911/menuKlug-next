'use client';
import { Menu } from "@/components/menu";

export default function MenuPage() {
  return (
    <main className="min-h-screen bg-gradient-to-r bg-slate-500 to-slate-800 py-12">
      <Menu />
    </main>
  )
}